/*eslint dot-notation: "off"*/
import {replace, when, verify} from '../../test-helper';

describe.only('fetch ', () => {
  it('retrieves payment for current month');
  it('retrieves payment for prior month');
});
